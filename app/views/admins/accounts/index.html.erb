<p id="notice"><%= notice %></p>

<h1>Accounts</h1>

<table>
  <thead>
  <tr>
    <th>User Name</th>
    <th>Number</th>
    <th>Amount</th>
    <th>Expiry</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @accounts.each do |account| %>
    <tr>
      <td><%= account.admin.full_name %></td>
      <td><%= account.number %></td>
      <td><%= account.amount %></td>
      <td><%= account.expiry %></td>
      <td><%= link_to 'Show', [:admins, account] %></td>
      <td><%= link_to 'Edit', edit_admins_account_path(account) %></td>
      <td><%= link_to 'Destroy', [:admins, account], method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Account', new_admins_account_path %>
